# 本棚アプリ 精査結果と改善計画表

2026-08-18 時点の main（コミット 5552839）を全ファイル精査した結果。
対象: index.html（約1600行）/ sw.js / manifest.json

---

## 1. 現状の評価

- 単一HTMLのPWAとして完成度が高い。設計判断がコメントに残っており保守しやすい。
- XSS対策（`esc()` / `textContent`）はほぼ一貫して守られている。
- Service Worker の方針（ページはネットワーク優先・書誌APIは非キャッシュ）は正しい。
- 一方で、**表示が壊れるCSSバグ1件、並び替えが壊れるデータ形式の不統一1件、
  データ全消失につながる起動時の挙動1件**を確認した。これらが最優先。

---

## 2. 見つかった不具合

### P0 — 目に見える実害があるもの

| # | 症状 | 原因（場所） | 直し方 | 規模 |
|---|------|------------|--------|------|
| 1 | 「書き出しておきませんか」の催促ボタンが画面中央に浮いて他の要素に重なる | `.hint` クラスが2回定義されている。書き出し催促用（index.html:117）とスキャナの案内用（index.html:145、`position:absolute` 付き）が衝突し、後者が勝つ | スキャナ側を `.scanhint` に改名し、HTML（index.html:267）と `stopScan` 内の参照も追随 | 小 |
| 2 | 「刊行が新しい順／古い順」の並びが崩れる | 刊行日の形式が経路で違う。国会図書館経由は `ym()` で `"201511"` に揃うが（index.html:457）、openBD 経由は `pubdate` を生のまま保存（index.html:381）。`"2015-11-09"` は `+d` が NaN になり、`"20151109"` は桁が多く常に「新しい」扱いになる | `fetchByISBN` の openBD 分岐でも `ym()` を通す。さらに `normalize()` に `b.date=ym(b.date)` を足して既存データを起動時に移行 | 小 |
| 3 | 保存データが壊れていた場合、起動しただけで全消去される | `store.get` は JSON 解析失敗時に null を返し（index.html:283-287）、起動処理が `books=[]` のまま無条件に `save()` する（index.html:1602）。壊れた（＝直せたかもしれない）データを空配列で上書きしてしまう | 読み込みが「失敗」なのか「未保存」なのかを区別する。解析失敗時は生の文字列を `bookshelf:broken` などへ退避してから進み、起動時の無条件 `save()` をやめる（読み込み成功時のみ保存） | 小〜中 |

### P1 — 条件付きだが実害があるもの

| # | 症状 | 原因（場所） | 直し方 | 規模 |
|---|------|------------|--------|------|
| 4 | 完結フラグと並び順設定が JSON 書き出しに含まれず、機種変更・復元で失われる。プレビュー環境（window.storage）では localStorage 直書きのためそもそも保存されない | 完結は `bookshelf:done`（index.html:1067-1077）、並び順は `bookshelf:sort`（index.html:779）を localStorage に直書き | 書き出しを `{books, done, sort}` の形式に拡張（従来の配列形式の読み込みは互換維持）。保存は `store` 経由に統一 | 中 |
| 5 | 新刊チェックから追加した本に内部フィールド `_series` `_from` が永続化される | `checkNew` が表示用に足したプロパティ（index.html:1231）が `add(..., {rec:x})` でそのまま保存される（index.html:529） | `add()` で `_` 始まりのキーを落とす。`normalize()` でも既存分を掃除 | 小 |
| 6 | USBスキャン・検索で失敗時の通知が二重に出る | `add()` の「ISBN の情報が見つかりません」トーストが `quiet` 指定でも出る（index.html:530） | `if(!opts.quiet) toast(...)` に変更。呼び出し側の表示はそのまま | 小 |
| 7 | 絞り込み検索が全角半角ゆれを拾えない（「Re:ゼロ」で「Ｒｅ：ゼロ」が出ない） | `hit()` が `norm()` を通さず `toLowerCase()` のみ（index.html:634-641） | 検索語と対象の両方を `norm().toLowerCase()` で比較。対象に `b.series` も含める | 小 |
| 8 | 手入力（著者名なし）のシリーズで「最新巻まで埋める」が常に空振りし、しかも「すでに全巻あります」と誤案内 | `fillSeries` の `akey(x.author)===akey(g.author)` が空文字と実著者で不一致（index.html:1092-1097） | `g.author` が空なら著者フィルタを外す（searchSeries が本編著者を選別済み）。0件時の文言を分岐 | 小 |
| 9 | series 有り・vol 無しの本が複数あると、1冊しか表示されない | 単巻グリッドが `g.items[0]` だけ描画（index.html:687）。通常フローでは発生しないが、手編集した JSON の読み込みで起こり得る | 単巻グループは `g.items` 全件を描画するか、`normalize()` で「vol の無い series」を消して単巻化する | 小 |
| 10 | 統計の著者数えで「著者A／著者B」形式が分割されない | メニューの著者分割正規表現に全角 `／` が無い（index.html:1484） | `akey` と同じ `/[、,\/／]/` に揃える | 小 |

### P2 — 軽微・特定条件のみ

- ユーザーが入力した表紙URLに空白が含まれると、フォールバック連鎖（`data-n` を空白区切りで持つ仕組み、index.html:747-748）が壊れる。区切りを `\n` などに変更。
- `hunt()` は `searchBooks`（cnt=200）を使うため、版が多い長期シリーズで巻を取りこぼす可能性。`searchSeries`（cnt=500＋著者選別）に寄せる。
- Service Worker の表紙キャッシュに上限がなく、無限に増える。件数上限＋古いものから削除を追加。
- ISBN-10 → 13 変換（index.html:327-333）が ISBN-10 側のチェックデジットを検証しない。

---

## 3. 改善提案（バグではないが、良くなるもの）

| 項目 | 内容 | 理由 | 規模 |
|------|------|------|------|
| 削除の取り消し | 「棚から出す」直後のトーストに「元に戻す」を付ける（5秒程度） | 現状ワンタップで即削除・復元手段なし | 小 |
| README.md 追加 | 使い方・GitHub Pages への置き方・データの控え（書き出し）の重要性を記す | リポジトリに説明が一切ない。将来の自分のため | 小 |
| iOS 対応の案内 | BarcodeDetector 非対応端末（iOS Safari 全般）では「USBリーダー」画面に回るが、iPhone に USB リーダーはまず無い。「探す」への誘導文言を足すか、zxing-wasm 導入を検討（外部依存が増えるトレードオフあり） | iPhone 利用者はカメラスキャンできないことに気づけない | 小（案内）／大（ライブラリ） |
| アクセシビリティ | 星評価・持ちかた切替などに aria-label / aria-pressed を付与 | ボタンが記号のみでスクリーンリーダーに伝わらない | 小 |
| isDone のキャッシュ | 描画のたびに localStorage を読んで JSON 解析している（index.html:1068） | 棚が育つと描画ごとのコストが積む | 小 |
| 日付の月表示 | 詳細画面は年のみ表示。#2 修正で月が揃うので「2015年11月」まで出せる | 情報が既にあるのに捨てている | 小 |

---

## 4. 拡張候補（やるかどうかの判断が必要）

| 候補 | 概要 | 使う既存資産 | 規模 |
|------|------|------------|------|
| 読書統計 | 月別読了数・積読数・年間ペース。「この棚のこと」に追加 | `readAt`（既に記録している） | 中 |
| 新刊チェックの半自動化 | 前回チェックから7日以上たっていたら起動時に裏で実行し、見つかったらヘッダに印を出す。手動実行は残す | `checkNew` / 完結フラグ | 中 |
| 昇順・降順トグル | 並び替えメニューで同じ項目をもう一度押すと反転 | `SORTS` / `applySort` | 小 |
| 著者で絞り込み | 詳細画面の著者名タップで検索欄に反映 | 既存の絞り込み | 小 |
| CSV 読み込み | 書き出しは CSV 対応済みだが読み込みは JSON のみ。表計算で編集→戻す運用を可能に | `dl` / `normalize` | 中 |
| 貸出管理 | 「貸出中（誰に・いつから）」を専用フィールドに。現状は note で代用 | 詳細シート | 中 |
| タグ | 自由タグ＋チップでの絞り込み | チップUI | 中 |
| Web Share API | スマホで書き出しファイルを共有シートに渡す（保存先を選びやすく） | `dl` | 小 |
| 発売予定の把握 | 国会図書館は発売前データが弱い。楽天ブックスAPI等の併用（要APIキー・要検討） | `checkNew` | 大 |

---

## 5. 実施計画表

| フェーズ | 内容 | 含む項目 | 目安 | 依存 |
|---------|------|---------|------|------|
| **1. 壊れているものを直す** | 表示・並び替え・データ保全の修正 | P0 #1〜#3 | 半日 | なし |
| **2. データの整合を取る** | 書き出し形式の拡張と内部フィールドの掃除 | P1 #4〜#6 | 半日 | #3（保存まわりを触るため先に） |
| **3. 細かい不具合の一掃** | 検索ゆれ・fillSeries・描画漏れ・統計 | P1 #7〜#10、P2 全件 | 半日〜1日 | なし |
| **4. 使い勝手の底上げ** | Undo・README・iOS案内・aria・月表示 | 改善提案 全件 | 1日 | なし |
| **5. 拡張（選定して着手）** | 上の拡張候補から2〜3個。おすすめは「読書統計」「昇順降順」「新刊チェック半自動化」 | 拡張候補 | 項目ごと | フェーズ2（統計は readAt / 書き出し形式に依存） |

## 6. 実施記録（2026-08-18）

### 追記2（実データでの検証で判明した構造的な不具合）

利用者の実際の書き出し（89冊）で確認した2件。どちらも国会図書館のデータの癖に起因する。

1. **巻の途中で見出しが変わる作品の巻が永久に足せない**
   「電波教師」は1〜4巻と9巻が副題なし、5巻以降が「電波教師 : HE IS A ULTIMATE TEACHER」という
   別の見出しで登録されている。見出しの完全一致で絞っていたため、抜けている5巻ぶんが
   検索にも巻ピッカーにも出てこなかった。
   → 副題（" : " 以降）を落とした**主題名**で突き合わせるようにした。棚の束ね鍵も主題名にしたので、
   両方の見出しが1つの棚に並ぶ。副題ちがいの見出しからは「まだ無い巻だけ」補うので、
   外伝（「オーバーロード : カッツェ平野の幽霊船」など）が本編の巻を上書きすることはない。
   空白なしのコロン（「Re:ゼロから始める異世界生活」）は題名の一部なので切らない。

2. **小説版と漫画版が同じ束に混ざり、片方の巻が消える**
   「超人高校生たちは異世界でも余裕で生き抜くようです!」は漫画版でも原作者（海空りく）だけが
   返るため、著者では版を分けられない。同じ3巻が2冊あり、片方が棚から見えなくなっていた。
   → **出版社**で版を分ける。ただし「オーバーロード」のようにエンターブレイン→KADOKAWA と
   版元が変わっただけの作品を割ってはいけないので、**巻番号が重なるときにだけ**別の版と見なす。
   実データで小説（SBクリエイティブ9冊）と漫画（スクウェア・エニックス5冊）に正しく分かれ、
   オーバーロード19巻は1束のまま保たれた。

あわせて修正した副産物:
- 同じ巻が複数ある場合、はみ出したぶんを捨てずに保持し、シリーズ画面に「同じ巻が重なっています」として出す。
- 巻ピッカーで、他の版にしか無い巻を「別の版にあります」と案内する。
- 版が分かれた束で他の版を見たとき、全部「持っている」判定になっていた不具合を修正。
- 検索結果の一括追加が、著者の別作品（「幼女戦記」→「売国機関」）を候補にしていた。探した題名と一致する作品だけに限定。

### 追記（同日・利用者フィードバック対応）

- 一括追加の高速化（1冊ごと→最後に1回の保存・描画）、入力サジェスト、原題（英語）検索、巻範囲プルダウン、まとめて入力の2段階化を実施。
- **巻ピッカー**を新設（シリーズ画面「巻を足す」）。3巻だけ持っている状態から1・2巻を選んで足せる。hunt / fillSeries はこれに統合して廃止。
- **版（小説/漫画）の選択**を追加。巻ピッカー・まとめて入力・検索結果の一括追加で、著者ごとの版か「両方」を選べる。所持判定も版ごとに行う。
- **操作の記録**を追加（端末内のみ・直近500件・メニューから閲覧/書き出し/消去）。
- **検索の高速化**：同一クエリ10分キャッシュ＋英数字クエリは題名/全項目を並列取得＋429自動リトライ。
- 副産物の不具合修正：国会図書館の publicationPlace="ja"（小文字）の本を誤って捨てていた。

- **フェーズ1〜4: 実施済み。** P0 #1〜#3、P1 #4〜#10、P2 全件、改善提案全件を実装した。
- **フェーズ5: 一部実施。** 読書統計（積読・今年の読了）、昇順降順トグル、著者タップで絞り込みを実装。
  未実施は「新刊チェックの半自動化」「CSV読み込み」「貸出管理」「タグ」「Web Share API」「発売予定の把握」。
- 補足: 並び順の設定・完結の印・書き出しの記録は `bookshelf:meta` に統合した（旧 localStorage キーからは初回起動時に自動で引き継ぐ）。JSON 書き出しは `{app, version, books, done, sort}` 形式になったが、旧形式（配列のみ）の読み込みも受け付ける。

### 進め方のメモ

- フェーズ1は3件とも互いに独立しており、1コミットずつ分けられる。
- #2（日付）と #4（書き出し形式）はデータ移行を含むので、修正前に一度 JSON 書き出しで控えを取ってから作業する。
- 拡張のうち「発売予定の把握」だけは外部APIキーが要るため、他と切り離して判断する。
- 単一ファイル構成は維持する（このアプリの設計思想。ビルド不要・GitHub Pages 直置きの利点を守る）。
